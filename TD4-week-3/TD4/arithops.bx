var n = 0, i = 1 : int; 
var m = 1 : int; 

def main()
{
    var x = 2 : int; 
    var res = 0 : int; 
    m = m + n; 
    n = m;
    print(n); 
    print(m);
    var a = true : bool; 
    res = foo(x, n, a); 
    print(a); 
    print(res); 
    return; 
}

def foo(x, y : int, a : bool) : int
{
    print(x);
    print(n);
    return x + n;
}
